# 扶養親族資格確認調査書（扶養手当）印刷システム 仕様書

- バージョン: v0.2（ドラフト／仕様固め中）
- 作成日: 2026-09-14
- ステータス: **様式未確定**（後述「4. 様式・印字項目」参照）。入力データ構造は実サンプルにより確定。

---

## 1. 背景・目的

### 1.1 根拠資料

本システムは、**陸前高田市**（市レベル）の下記内部通知に基づく事務を支援する。

| 項目 | 内容 |
|---|---|
| 文書名 | 令和８年度被扶養者及び扶養親族資格確認調査の実施について（通知） |
| 文書番号 | 陸高総号外 |
| 日付 | 令和８年８月３日 |
| 発出 | 陸前高田市 総務部総務課長 |
| 宛先 | 各部課等の長 |

上記は市の**内部通知（事務連絡）**であり、条例・規則ではない。

### 1.2 未確認事項（法令根拠）

扶養手当そのものの根拠規定（陸前高田市職員の給与に関する条例の扶養手当に係る条文、
及び同施行規則における扶養親族の届出・認定手続の条文）は、**本セッションから
例規集（条例規則/現行フォルダ）を参照できないため未確認**である。推測での引用は行わない。

仕様確定前に、以下を例規集で確認し本書に条文番号とノート名を追記すること。

- 陸前高田市（市）: 職員の給与に関する条例 — 扶養手当の支給要件・扶養親族の範囲
- 陸前高田市（市）: 同施行規則等 — 扶養親族届・認定・資格確認の手続
- 参考: 国の一般職の職員の給与に関する法律（国レベル）の扶養手当規定との異同

なお、別紙１の「被扶養者資格確認調査」は**共済組合（市とは別の団体）**の制度であり、
市の扶養手当とは根拠が異なる。混同しないこと。本システムの対象は**扶養手当のみ**。

### 1.3 目的

職員係が、扶養手当の資格確認調査に用いる「扶養親族確認調査書」を、
人事給与システムから出力した対象者データをもとに**事前印字した状態で人数分印刷**できるようにする。

---

## 2. スコープ

### 2.1 対象

- 扶養手当に係る「**扶養親族確認調査書**」の帳票印刷
- 対象者の抽出（基準日・生年月日による絞り込み）
- 調査書への職員情報・扶養親族情報の事前印字
- 添付書類チェックリストの印字（別紙の判定ルールに基づく）

### 2.2 対象外（本フェーズ）

- 共済組合の「被扶養者資格確認調査書」の印刷
- 提出状況の進捗管理・督促
- 回収後の審査結果の登録・返納処理
- 人事給与システムへの書き戻し

---

## 3. 対象者抽出要件

### 3.1 年度パラメータ

対象年度を西暦4桁 `FY` で指定する（令和8年度 = 2026、令和9年度 = 2027）。

```
生年月日上限 = date(FY - 18, 4, 1)
```

| 年度 | FY | 生年月日上限 | 通知の表記 |
|---|---|---|---|
| 令和８年度 | 2026 | 2008-04-01 | 平成２０年４月１日以前の出生者 |
| 令和９年度 | 2027 | 2009-04-01 | 平成２１年４月１日以前の出生者 |

`FY - 18` で毎年度自動計算されるため、年度を切り替えるだけで運用できる。
生年月日上限をハードコードしてはならない。

なお、対象は「子」に限らず**扶養親族全員**（配偶者・父母等を含む）である。
通知【別紙】２⑴も「平成２０年４月１日以前の出生者」と親族種別を限定していない。

### 3.2 抽出手順

```
STEP1  在職者の抽出（名簿データ）
         名簿.在職 = 1

STEP2  対象扶養親族の抽出（扶養データ）
         扶養.year         = FY
     AND 扶養.teate        = 1
     AND 扶養.temp_kingaku > 0          -- 0 および NULL は対象外
     AND 扶養.birthday    <= 生年月日上限

STEP3  結合
         扶養.number = 名簿.番号
         （STEP1 の在職者に紐づく扶養親族のみを印刷対象とする）

STEP4  並び順
         名簿.所属CD, 名簿.係CD, 名簿.番号, 扶養.temp_jyun
```

### 3.3 除外・警告の扱い

| ケース | 扱い |
|---|---|
| 扶養データに存在するが名簿に該当 `番号` がない | **警告一覧に出力**（退職者の残データ等の可能性）。印刷対象外 |
| 名簿に在職者として存在するが対象扶養親族が0件 | 印刷対象外（正常。件数のみ表示） |
| `teate` = 1 だが `temp_kingaku` = 0 | 対象外（正常） |
| `birthday` が空 | **エラー一覧に出力**。印刷対象外 |

### 3.4 未確定の判定条件

- `在職` 列が取り得る値（1 以外に何があるか）は未確認。
  また、調査基準日（令和８年６月１日）時点の在職で判定すべきか、
  名簿出力時点の在職で判定すればよいかも未確認。
  前者の場合は `採用日` / `退職日` による判定を追加する。
- 扶養データの `month` 列の意味が不明（サンプル値 `90`）。
  同一 `number` × 同一扶養親族について `year` / `month` 違いの履歴レコードが
  存在する場合、**どのレコードを採用するかを確定しないと重複印字が発生する**。

## 4. 様式・印字項目

> **【要対応】** 添付いただいた `02_...doc` はバイナリ解析の結果、全4ページすべてが
> 通知文と別紙であり、**調査書の様式は含まれていなかった**（本文2,080字＋ヘッダー13字＋
> テキストボックス21字で全量、埋め込みオブジェクトなし）。
>
> 「扶養親族確認調査書」の様式ファイルの提供を受けてから、本章を確定する。

確定待ちの項目:

- 用紙サイズ・向き（A4縦想定）、余白
- 1枚あたりの単位（職員1人1枚か、扶養親族1人1枚か）
- 扶養親族欄の行数（1枚に何人まで書けるか → 超過時の2枚目の扱い）
- 各記入欄の項目名・並び・罫線レイアウト
- 押印欄・所属長確認欄の有無
- 提出期限や問合せ先の刷り込み文言

---

## 5. 入力データ仕様

### 5.1 方式

人事給与システム等から出力した **Excel（.xlsx）2ファイル**を本システムに取り込む。

| # | ファイル | 役割 |
|---|---|---|
| 1 | 名簿データ | 在職判定、職員の氏名・所属・職名の取得 |
| 2 | 扶養データ | 扶養親族の抽出、氏名・生年月日・続柄の取得 |

- ヘッダ行（1行目）の**列名で対応付ける**。列順・列数の増減に依存しない
- CSV 形式での提供に切り替わった場合にも対応できるよう、
  読み取り層を Excel / CSV で差し替え可能な構造にする
  （CSV の場合は CP932 / UTF-8(BOM付) を自動判別）

### 5.2 名簿データ（41列）

結合キー・判定・印字に使う列は以下。その他の列（PASS、社保、Mail、住所、電話、
緊急時連絡先等）は**個人情報保護の観点から取り込まない**。

| 列名 | 型 | 用途 | 備考 |
|---|---|---|---|
| `番号` | 数値 | **結合キー** | 扶養データ `number` と対応 |
| `在職` | 数値 | **在職判定** | 1 = 在職 |
| `氏名` | 文字列 | 印字 | 姓名は全角スペース区切り |
| `ﾌﾘｶﾞﾅ` | 文字列 | 印字（ふりがな欄がある場合） | 半角カナ。印字時は全角変換を検討 |
| `所属CD` / `所属名` | 数値 / 文字列 | 並び順・仕分け・印字 | 例: 1100 / 総務部総務課 |
| `係CD` / `係名` | 数値 / 文字列 | 並び順・仕分け・印字 | 例: 1100010 / 職員係 |
| `職名` | 文字列 | 印字 | 例: 主事 |
| `生年月日` | **Excelシリアル値** | （参考） | 下記5.4参照 |
| `採用日` / `退職日` | **Excelシリアル値** | 在職判定を厳密化する場合に使用 | 下記5.4参照 |
| `区分名` / `身分` / `職種` | 文字列 | 対象範囲の絞り込みに使う可能性 | 例: 正職員 / 一般行政職 |

### 5.3 扶養データ（21列）

| 列名 | 型 | 用途 | 備考 |
|---|---|---|---|
| `t_huyouid` | 数値 | レコード識別子 | |
| `number` | 数値 | **結合キー** | 名簿 `番号` と対応 |
| `year` | 数値 | **年度絞り込み** | 西暦4桁。2026 = 令和8年度 |
| `month` | 数値 | **意味未確認** | サンプル値 90。→ 9.未決事項 |
| `kanji` / `kana` | 文字列 | 印字 | 扶養親族の氏名 |
| `birthday` | 日付 | **対象判定・印字** | |
| `zokugara` | 数値 | 続柄コード | **コード表未入手**（サンプル値 31） |
| `zokugara2` | 数値 | 続柄コード（細分？） | **コード表未入手**（サンプル値 3002） |
| `teate` | 数値 | **扶養手当対象フラグ** | 1 = 対象 |
| `temp_kingaku` | 数値 | **扶養手当月額** | 0 は対象外。サンプル値 6500 |
| `temp_jyun` | 数値 | 順位（並び順に使用） | サンプル値 3 |
| `temp_kasan` | 数値 | 加算区分（用途未確認） | サンプル値 0 |
| `jidou` | 数値 | 児童手当関係フラグ | 本システムでは未使用 |
| `zei` / `zei_syogai` / `zei_doukyo` | 数値 | 税扶養関係フラグ | **7.7 の注意書きを参照** |
| `temp_zoku` / `temp_tosi_kubun` / `bikou` / `syotoku` | - | サンプルは全て空 | 用途未確認 |

### 5.4 日付の取り扱い（実装上の注意）

サンプルを解析した結果、**2ファイルで日付の持ち方が異なる**。

| ファイル | 列 | 実際の格納形式 |
|---|---|---|
| 名簿 | `生年月日` `採用日` `退職日` | **数値（Excelシリアル値）**、書式は General |
| 扶養 | `birthday` | 日付型（datetime） |

名簿側はシリアル値のため、**1900日付システム（基準日 1899-12-30）で日付に変換**する。

```
36602 → 2000-03-17
45017 → 2023-04-01
```

取込処理は「日付型ならそのまま／数値ならシリアル値として変換／文字列なら日付パース」の
3通りを受け付ける共通関数を用意する。空欄は NULL として扱う。

### 5.5 バリデーション

| 検査 | 対象 | 措置 |
|---|---|---|
| 結合キーの欠損 | `番号` / `number` | エラー一覧。印刷対象外 |
| `birthday` の欠損・変換不能 | 扶養 | エラー一覧。印刷対象外 |
| 名簿に該当 `番号` なし | 扶養 | 警告一覧。印刷対象外 |
| 同一職員に同名・同生年月日の扶養親族が重複 | 扶養 | 警告一覧。1件に集約せず全件表示し判断を仰ぐ |

### 5.6 サンプルデータの状況

現時点で提供を受けたサンプルは**各ファイル1行のみ**（名簿 `番号`=1007、扶養 `number`=1000）で、
両者が結合しないダミーデータである。結合・重複・履歴の挙動は検証できていない。
**複数職員・複数扶養親族を含むサンプル（個人情報はダミー化）**の提供を受けて確認したい。

## 6. 出力・印刷仕様

### 6.1 方式

**ローカルWebアプリ（HTML → ブラウザ印刷）**

- ローカルでサーバを起動し、ブラウザで操作する
- 様式は HTML + CSS（`@page` / `size: A4` / `mm` 単位）で再現する
- 印刷はブラウザの印刷機能（Ctrl+P）を使用。「PDFとして保存」も可能
- 外部ネットワーク送信は行わない（個人情報を扱うため、完全にローカル完結）

### 6.2 印刷要件

- 対象者全員分を**1つのHTMLに連続出力**し、一括印刷できること
- 1件＝1ページとし、`page-break-after` でページを分割する
- 所属順・職員番号順に並べ、所属ごとの仕分けができること
- ブラウザのヘッダ・フッタ（URL、ページ番号）が出ない前提でレイアウトする旨を操作手順に明記
- 印刷プレビューと実印刷でズレが出ないよう、絶対単位（mm/pt）でレイアウトする

### 6.3 画面構成（案）

1. **取込画面** — CSVを選択して取り込み、件数とエラー一覧を表示
2. **条件設定画面** — 基準日・生年月日上限・対象年度を入力
3. **対象者一覧画面** — 抽出結果を確認、所属や個人単位で印刷対象を絞り込み
4. **印刷プレビュー画面** — 調査書を人数分レンダリング、そのまま印刷

---

## 7. 添付書類チェックリストの判定ロジック

通知【別紙】２⑶「添付書類 … 被扶養者資格確認調査と同じ。ただし、被扶養者資格確認調査と
重複している添付書類については添付の必要無し。」に基づき、別紙１⑸のルールを準用する。

### 7.1 全員

| 条件 | 添付書類 |
|---|---|
| 原則全員（無職無収入でも必要） | 所得証明書 |
| ただし**全日制の学校等の学生**は除く | — |

※「全日制の学校等」＝学校教育法に規定する学校、これに相当する海外の学校、
高校・大学等への入学を目的とした予備校。**定時制・通信制課程・夜間課程の学生等は要提出。**

### 7.2 収入がある場合

| 収入の内容 | 添付書類 |
|---|---|
| a 給与・報酬等 | 就労証明書（令和７年７月〜令和８年６月に1日でも就労していれば必要。様式を印刷し事業所から証明を受ける。**事業所の証明印の押印必須**） |
| b 年金 | 最新の年金改定通知書 又は 年金振込通知書の写 |
| c 事業収入（農業・自営業等） | 令和７年分の確定申告書及び収支内訳書の写 |
| d その他収入 | 当該収入を確認できる書類 |

### 7.3 18歳以上の全日制の学生（同居・別居とも）

- 在学証明書（令和８年４月１日以降交付のもの、**写不可**）

### 7.4 別居している場合

| 区分 | 添付書類 |
|---|---|
| a 全員（学生・施設入所者を除く。**定時制等の学生は要提出**） | 別居先世帯全員分の住民票／令和７年７月〜令和８年６月の各月の送金実績証明書（振込人・受取人の両方の氏名が記載されたもの。通帳の写等） |
| b 身体障害者授産施設、知的障害者更生施設、介護老人保健施設 入所者等 | 施設入所証明書の写 |
| c 養護老人ホーム、特別養護老人ホーム、児童福祉施設等 入所者 | 施設入所証明書の写／入所費用が確認できる書類／組合員が援助していることを客観的に証明できる書類（通帳の写等） |

### 7.5 組合員以外に優先扶養義務者がいる場合

（例: 母であれば父、孫であれば子 等）

- 優先扶養義務者の所得証明書
- 優先扶養義務者等の収入に関する証明書（収入がある場合）
- 世帯全員の住民票
  - ただし父母がともに被扶養者となっている場合、住民票は添付不要
  - 優先扶養義務者が後期高齢者医療制度の被保険者の場合、当該保険証の添付をもって住民票に代えられる

### 7.6 留意事項

- 事業所の倒産等により就労証明書が提出できない場合は申立書が必要（担当へ連絡）

### 7.7 実装上の扱い（v0.2で方針変更）

**添付書類の自動判定は行わない。チェックボックス付きの一覧を全件印字する方式とする。**

理由: 提供を受けた扶養データ（21列）を確認した結果、判定に必要な以下の情報が
**いずれも存在しない**ため。

| 判定に必要な情報 | 扶養データでの有無 |
|---|---|
| 同居・別居の区分 | **なし** |
| 収入区分（給与報酬／年金／事業／その他／無収入） | **なし**（`syotoku` 列はサンプルが空で用途未確認） |
| 学生区分（全日制／定時制・通信制・夜間） | **なし** |
| 施設入所区分（別紙の a / b / c） | **なし** |
| 優先扶養義務者の有無 | **なし** |

> 注意: `zei_doukyo` は列名から**税法上の同居老親等の判定フラグ**と推測されるが、
> これは所得税法上の概念であり、共済組合の被扶養者認定や扶養手当における
> 同居・別居の判定とは**別の概念**である。同居・別居の判定には流用しない。
> （`zei_doukyo` の正確な定義は未確認。人事給与システムの仕様確認が必要）

したがって調査書には次のとおり印字する。

- 7.1〜7.5 の添付書類を**チェックボックス付きの一覧として全件印字**し、
  職員が該当欄にチェック・記入する
- 年・期間の表記（「令和７年７月〜令和８年６月」「令和７年分」「令和８年４月１日以降交付」等）は
  **年度パラメータ `FY` から自動計算して印字**する。ハードコードしない
- `FY` から計算する主な値:

| 印字箇所 | 計算式（FY = 2026 の場合） |
|---|---|
| 就労証明書・送金実績の対象期間 | (FY-1)年7月 〜 FY年6月 → 令和７年７月〜令和８年６月 |
| 確定申告書の年分 | (FY-1)年分 → 令和７年分 |
| 在学証明書の交付日要件 | FY年4月1日以降 → 令和８年４月１日以降 |
| 調査基準日 | FY年6月1日 → 令和８年６月１日 |

なお `syotoku` 列に収入区分や所得額が入る運用であれば自動判定の余地があるため、
同列の仕様確認を未決事項に含める。

## 8. 非機能要件

| 項目 | 内容 |
|---|---|
| 個人情報 | 外部送信なし。ローカル完結。取込データはメモリ上のみで保持し、既定では永続化しない |
| 実行環境 | 庁内Windows PC。ブラウザは Microsoft Edge を想定 |
| 想定件数 | 数十〜数百件（要確認） |
| 年度更新 | 基準日・生年月日上限・年度表記を設定画面から変更でき、翌年度もコード修正なしで使えること |

---

## 9. 未決事項

### 9.1 提供待ち

| # | 事項 | 影響 |
|---|---|---|
| 1 | **扶養親族確認調査書の様式ファイル** | 印字項目・レイアウトが確定できない（最優先） |
| 2 | **複数行のサンプルデータ**（名簿・扶養とも、個人情報はダミー化） | 結合・重複・履歴の挙動が検証できない。現サンプルは各1行で相互に結合しない |
| 3 | **続柄コード表**（`zokugara` / `zokugara2`） | 調査書に続柄を印字できない。サンプル値は 31 / 3002 |

### 9.2 要確認（仕様判断）

| # | 事項 | 補足 |
|---|---|---|
| 4 | 扶養データ `month` 列の意味（サンプル値 `90`） | 履歴レコードが存在する場合の採用レコード決定に必要。**重複印字に直結** |
| 5 | `在職` 列の取り得る値、および在職判定を調査基準日時点で行うか | 基準日判定なら `採用日` / `退職日` を併用する |
| 6 | 対象とする `区分名` / `身分` の範囲 | 正職員のみか、会計年度任用職員等も含むか |
| 7 | `temp_kingaku` の定義（扶養手当の月額との理解でよいか） | サンプル値 6500 |
| 8 | `syotoku` 列の仕様 | 値が入る運用なら添付書類の一部自動判定が可能になる |
| 9 | `temp_kasan` / `temp_zoku` / `temp_tosi_kubun` / `bikou` の用途 | 印字要否の判断に必要 |
| 10 | 1枚あたりの扶養親族記入可能人数と超過時の扱い | 様式確定後に決定 |
| 11 | 就労証明書の様式も本システムで印刷するか | 別紙１⑸イ(ア)a に「別添就労証明書を印刷し」とある |
| 12 | 対象者の想定件数 | 性能・印刷運用の見積りに必要 |
| 13 | 庁内PCでのランタイム（Python / Node.js）導入可否 | 導入不可なら実装方式の再検討が必要 |

### 9.3 要確認（法令根拠）

| # | 事項 | 参照先 |
|---|---|---|
| 14 | 扶養手当の支給要件・扶養親族の範囲の条文 | 陸前高田市（市）職員の給与に関する条例 — **例規集で条文番号を確認のうえ本書に追記** |
| 15 | 扶養親族届・認定・資格確認の手続の条文 | 陸前高田市（市）同施行規則等 — 同上 |

## 10. 参考: 添付書類の例（通知 別紙３より）

**例１** 配偶者（専業主婦）、大学生の子（別居）、母（年金受給者・同居）を扶養親族としている場合

- 配偶者（専業主婦） … 所得証明書
- 大学生の子（別居） … 在学証明書
- 母（年金受給者・同居） … 所得証明書／年金改定通知書の写
- その他（父が優先扶養義務者となるため） … 父の所得証明書／父の収入に関する証明書／世帯全員分の住民票

**例２** 父母（年金受給者・別居）を扶養親族としている場合

- 父・母それぞれ … 所得証明書／年金改定通知書／別居先の世帯全員分の住民票
- その他 … 送金実績の分かる書類（通帳の写等）

**例３** 配偶者（パート）及び中学生の子を扶養親族としている場合

- 配偶者（パート） … 所得証明書／就労証明書
- 中学生の子 … なし（**本調査の対象外**）
